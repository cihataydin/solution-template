version: '3.8'

services:
  api:
    container_name: api
    image: cihataydin/api:latest
    build:
      context: .
      dockerfile: Dockerfile 
    ports:
      - "8080:80"
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
    restart: always
